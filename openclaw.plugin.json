{
  "id": "linear-light",
  "name": "Linear Light",
  "description": "Lightweight Linear integration \u2014 @mention triggers task sessions with status management and notifications",
  "version": "0.1.0",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "enabled": {
        "type": "boolean",
        "description": "Enable or disable the plugin",
        "default": true
      },
      "webhookSecret": {
        "type": "string",
        "description": "Linear webhook signing secret",
        "sensitive": true
      },
      "mentionTrigger": {
        "type": "string",
        "description": "Text that triggers the agent in comments (default: auto-detect from OAuth app)",
        "default": "Linus"
      },
      "autoInProgress": {
        "type": "boolean",
        "description": "Automatically set issue to In Progress when agent starts working",
        "default": true
      },
      "agentIdentity": {
        "type": "string",
        "description": "Custom system identity instruction injected into agent prompts. Default: 'You are OpenClaw, a Linear workflow assistant. Do not respond using a personal assistant identity (e.g. Linus). Available tools: linear_update_status (change status), linear_get_issue (view details), linear_search_issues (search). Important: Do not modify issue status (especially do not mark Done) unless explicitly requested by the user.'"
      },
      "initialResponseTemplate": {
        "type": "string",
        "description": "Template for initial activity response. Supports {identifier} and {title} placeholders.",
        "default": "Received, processing {identifier}: {title}"
      },
      "linearClientId": {
        "type": "string",
        "description": "Linear OAuth client ID (required for token refresh)",
        "sensitive": true
      },
      "linearClientSecret": {
        "type": "string",
        "description": "Linear OAuth client secret (required for token refresh)",
        "sensitive": true
      },
      "completionLoopEnabled": {
        "type": "boolean",
        "description": "Enable completion loop — prompts agent every interval if issue is not in a terminal state",
        "default": true
      },
      "completionLoopInterval": {
        "type": "number",
        "description": "Completion loop interval in minutes (minimum 1)",
        "default": 10
      },
      "completionLoopMaxIterations": {
        "type": "number",
        "description": "Maximum number of completion loop prompts before stopping (0 = unlimited)",
        "default": 0
      },
      "completionLoopPrompt": {
        "type": "string",
        "description": "Prompt template sent to agent on each loop tick. Supports {identifier}, {state}, {iteration} placeholders.",
        "default": "[Completion Check] The issue {identifier} is still in \"{state}\" state. Please continue working on it. If you believe the work is complete, use the linear_update_status tool to mark it as Done."
      }
    }
  }
}